{
	"heat-demand-total": { "title": "P1.1 Total heat demand", "combine": "sum", "units":"kWh", "dp": 0, "format": "function(v,u){ if(v>1e6){ return (v/1e6).toLocaleString()+'TWh'; } if(v>1e3){ return (v/1e3).toLocaleString()+'MWH'; } if(v==0){ return '0'; } return v.toLocaleString()+u; }", "description": "Heat demand in kWh of all buildings, including new build." },
	"heat-demand": { "title": "P1.2 Heat demand", "combine": "sum", "units":"kWh", "dp": 0, "format": "function(v,u){ if(v>1e6){ return (v/1e6).toLocaleString()+'TWh'; } if(v>1e3){ return (v/1e3).toLocaleString()+'MWH'; } if(v==0){ return '0'; } return v.toLocaleString()+u; }", "description": "Heat demand in kWh of all buildings existing in 2020 in each LSOA. New builds and other changes to non-domestic footprint over time are excluded here to isolate the effect the energy efficiency." },
	"electricity-demand-heat": { "title": "P1.3 Electricity demand for heat", "combine": "sum", "units":"kWh", "dp": 0, "format": "function(v,u){ if(v>1e6){ return (v/1e6).toLocaleString()+'TWh'; } if(v>1e3){ return (v/1e3).toLocaleString()+'MWH'; } if(v==0){ return '0'; } return v.toLocaleString()+u; }", "description": "Electricity demand for space and hot water heating by LSOA, including new builds and district heating" },
	"heat-demand-met-fossilfuel": { "title": "P1.4 Heating demand met by fossil fuel", "combine": "sum", "units":"kWh", "dp": 0, "format": "function(v,u){ if(v>1e6){ return (v/1e6).toLocaleString()+'TWh'; } if(v>1e3){ return (v/1e3).toLocaleString()+'MWH'; } if(v==0){ return '0'; } return v.toLocaleString()+u; }", "description": "" },
	"heat-demand-met-biomass": { "title": "P1.4 Heating demand met by biomass", "combine": "sum", "units":"kWh", "dp": 0, "format": "function(v,u){ if(v>1e6){ return (v/1e6).toLocaleString()+'TWh'; } if(v>1e3){ return (v/1e3).toLocaleString()+'MWH'; } if(v==0){ return '0'; } return v.toLocaleString()+u; }", "description": "" },
	"heat-demand-met-directelectric": { "title": "P1.4 Heating demand met by direct electric", "combine": "sum", "units":"kWh", "dp": 0, "format": "function(v,u){ if(v>1e6){ return (v/1e6).toLocaleString()+'TWh'; } if(v>1e3){ return (v/1e3).toLocaleString()+'MWH'; } if(v==0){ return '0'; } return v.toLocaleString()+u; }", "description": "" },
	"heat-demand-met-heatpump": { "title": "P1.4 Heating demand met by heat pump", "combine": "sum", "units":"kWh", "dp": 0, "format": "function(v,u){ if(v>1e6){ return (v/1e6).toLocaleString()+'TWh'; } if(v>1e3){ return (v/1e3).toLocaleString()+'MWH'; } if(v==0){ return '0'; } return v.toLocaleString()+u; }", "description": "" },
	"heat-demand-met-hybridhp": { "title": "P1.4 Heating demand met by hybrid HP", "combine": "sum", "units":"kWh", "dp": 0, "format": "function(v,u){ if(v>1e6){ return (v/1e6).toLocaleString()+'TWh'; } if(v>1e3){ return (v/1e3).toLocaleString()+'MWH'; } if(v==0){ return '0'; } return v.toLocaleString()+u; }", "description": "" },
	"heat-demand-met-hydrogen": { "title": "P1.4 Heating demand met by hydrogen", "combine": "sum", "units":"kWh", "dp": 0, "format": "function(v,u){ if(v>1e6){ return (v/1e6).toLocaleString()+'TWh'; } if(v>1e3){ return (v/1e3).toLocaleString()+'MWH'; } if(v==0){ return '0'; } return v.toLocaleString()+u; }", "description": "" },
	"domestic-new-build": { "title": "P2.5 Domestic new build projections", "combine": "sum", "units":"", "dp": 0, "description": "Cumulative number of domestic new builds per year." }
}
